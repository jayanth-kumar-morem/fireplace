# Rust server port (default: 3001)
PORT=3001

# Optional Rust log level (example: info,debug)
RUST_LOG=info

# Kalshi API credentials (optional — falls back to REST polling without these)
KALSHI_API_KEY=
KALSHI_PRIVATE_KEY_PATH=
